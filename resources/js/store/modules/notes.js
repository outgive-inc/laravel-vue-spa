import axios from 'axios';

const state = {
  notes: []
};

const getters = {
  allNotes: state => state.notes
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
};
